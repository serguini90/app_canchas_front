import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.canchas_sinteticas',
  appName: 'canchas_sinteticas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
